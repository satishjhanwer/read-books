import React from 'react';

const LoadingIndicator = () => (
  <div style={{ margin: '2em auto', width: '40px', height: '40px', position: 'relative' }}>Loading.....</div>
);

export default LoadingIndicator;
